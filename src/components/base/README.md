# Base Components

Reserve this folder for design-system level primitives only.

Examples:

- buttons
- badges
- inputs
- empty states

If a component still carries feature wording like `org`, `member`, or `payout`, it should usually live under `features/` instead.
